<?php

class PTZipcodeTestCase extends DrupalWebTestCase {
  /**
   * Implementation of getInfo()
   *
   */
  public static function getInfo() {
    return array(
      'name' => t('Portugal Zipcode test'),
      'description' => t('Tests various valid and invalid Portugal zipcodes for validity'),
      'group' => t('Zipcode')
    );
  }

  public function testZipcodePTValid() {
    //$this->assertTrue(valid_zipcode('pt', '(041)245-9848'), t('Test valid'));
  }

  public function testZipcodePTInvalid() {
    //$this->assertFalse(valid_zipcode('pt', '0412459848'), t('Test invalid '));
  }

  public function testZipcodePTFormatting() {
    //$this->assertEqual(format_zipcode('pt', '+6421123456', null), '+64 21 123 456', t('Check international mobile format'));
  }
}